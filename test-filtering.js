const testFiltering=()=>{const filters=document.querySelectorAll(".btn-filter");const items=document.querySelectorAll(".project-item");console.log("Testing project filtering system...");console.log("Initial state:");console.log("Visible items:",Array.from(items).filter(item=>!item.classList.contains("hidden")).length);filters.forEach(filter=>{const category=filter.getAttribute("data-filter");console.log(`\nTesting ${category} filter:`);filter.click();setTimeout(()=>{const visibleItems=Array.from(items).filter(item=>!item.classList.contains("hidden"));const expectedItems=category==="all"?items.length:Array.from(items).filter(item=>item.classList.contains(category)).length;console.log(`- Expected items: ${expectedItems}`);console.log(`- Actual visible items: ${visibleItems.length}`);console.log(`- Filter working correctly: ${visibleItems.length===expectedItems}`);},500);});}document.addEventListener("DOMContentLoaded",testFiltering);
